# opi_epaper_driver


1. Create a menu, so you can open/create a file, connect BT keyboard, connect to wifi, and set settings
-2. Create a File Handler for holding the file buffer and pointer to file on ROM
3. Create an input layer for handling input into the file and handling cursor/view on screen
4. Create screen that simply takes the current visual space of the file buffer and cursor
5. A draw system that takes in a screen and just displays it
